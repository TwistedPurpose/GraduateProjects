/*
Deployment script for gm_planner_db

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "gm_planner_db"
:setvar DefaultFilePrefix "gm_planner_db"
:setvar DefaultDataPath ""
:setvar DefaultLogPath ""

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
/*
The column SessionNumber on table [dbo].[Session] must be changed from NULL to NOT NULL. If the table contains data, the ALTER script may not work. To avoid this issue, you must add values to this column for all rows or mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[Session])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Dropping [dbo].[FK_Character_Session_SessionId]...';


GO
ALTER TABLE [dbo].[CharacterSessions] DROP CONSTRAINT [FK_Character_Session_SessionId];


GO
PRINT N'Dropping [dbo].[FK_Session_ToMap]...';


GO
ALTER TABLE [dbo].[Session] DROP CONSTRAINT [FK_Session_ToMap];


GO
PRINT N'Dropping [dbo].[FK_Session_ToCampaign]...';


GO
ALTER TABLE [dbo].[Session] DROP CONSTRAINT [FK_Session_ToCampaign];


GO
PRINT N'Starting rebuilding table [dbo].[Session]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_Session] (
    [Id]            INT            IDENTITY (1, 1) NOT NULL,
    [Title]         NVARCHAR (40)  NULL,
    [CampaignId]    INT            NULL,
    [BaseMapId]     INT            NULL,
    [SessionNumber] INT            NOT NULL,
    [Notes]         NVARCHAR (MAX) NULL,
    CONSTRAINT [tmp_ms_xx_constraint_PK_Session1] PRIMARY KEY CLUSTERED ([Id] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[Session])
    BEGIN
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_Session] ON;
        INSERT INTO [dbo].[tmp_ms_xx_Session] ([Id], [CampaignId], [BaseMapId], [SessionNumber], [Notes])
        SELECT   [Id],
                 [CampaignId],
                 [BaseMapId],
                 [SessionNumber],
                 [Notes]
        FROM     [dbo].[Session]
        ORDER BY [Id] ASC;
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_Session] OFF;
    END

DROP TABLE [dbo].[Session];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_Session]', N'Session';

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_constraint_PK_Session1]', N'PK_Session', N'OBJECT';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Creating [dbo].[FK_Character_Session_SessionId]...';


GO
ALTER TABLE [dbo].[CharacterSessions] WITH NOCHECK
    ADD CONSTRAINT [FK_Character_Session_SessionId] FOREIGN KEY ([SessionId]) REFERENCES [dbo].[Session] ([Id]);


GO
PRINT N'Creating [dbo].[FK_Session_ToMap]...';


GO
ALTER TABLE [dbo].[Session] WITH NOCHECK
    ADD CONSTRAINT [FK_Session_ToMap] FOREIGN KEY ([BaseMapId]) REFERENCES [dbo].[Map] ([Id]);


GO
PRINT N'Creating [dbo].[FK_Session_ToCampaign]...';


GO
ALTER TABLE [dbo].[Session] WITH NOCHECK
    ADD CONSTRAINT [FK_Session_ToCampaign] FOREIGN KEY ([CampaignId]) REFERENCES [dbo].[Campaign] ([Id]);


GO
/*
Post-Deployment Script Template							
--------------------------------------------------------------------------------------
 This file contains SQL statements that will be appended to the build script.		
 Use SQLCMD syntax to include a file in the post-deployment script.			
 Example:      :r .\myfile.sql								
 Use SQLCMD syntax to reference a variable in the post-deployment script.		
 Example:      :setvar TableName MyTable							
               SELECT * FROM [$(TableName)]					
--------------------------------------------------------------------------------------
*/

insert into History values('A sweet history');
insert into History values('The hero of Seattle, the one they call... Eric!');

insert into Campaign values('Dr. Larson and The Mystery of the Green Tomb',1);
insert into Campaign values('Dr. Larson and The Longest Knight',2);

GO

GO
